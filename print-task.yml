platform: linux

image_resource:
  type: registry-image
  source:
    repository: python
    tag: "3.10"

inputs:
- name: print-test

run:
  path: sh
  args:
    - -c
    - |
      cd print-test
      python script1.py
     
